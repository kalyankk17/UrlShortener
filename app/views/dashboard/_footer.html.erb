<div class="card border-0 bg-light shadow-sm rounded-4 mb-5">
    <div class="card-body p-4 p-md-5 text-center">
        <h2 class="h2 text-secondary mb-4">
            Want More? Try Premium Features!
        </h2>

        <p class="text-muted mb-4">
            Custom short links, powerful dashboard, detailed analytics, API, UTM builder, QR codes,<br class="d-none d-md-block">
            browser extension, app integrations and support
        </p>

        <% if user_signed_in? %>
            <div class="d-flex justify-content-center">
                <div class="me-4">
                    <%= link_to 'View all shortened urls', shortened_urls_show_path, class: 'btn btn-primary btn-lg px-5' %>
                </div>
                <div class="me-4">
                    <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-lg px-5 btn-danger" %>
                </div>
            </div>
        <% else %>
            <%= button_to 'Create Account', new_user_session_path, class: 'btn btn-success btn-lg px-5' %>
        <% end %>
    </div>
</div>
